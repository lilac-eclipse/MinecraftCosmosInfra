package com.lilaceclipse.cosmos

import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

class SampleTest {
    @Test
    fun testSomething() {
        val result = "expected result"
        assertEquals("expected result", result)
    }
}